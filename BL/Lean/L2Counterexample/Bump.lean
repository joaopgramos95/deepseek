import L2Counterexample.Basic

/-!
# Smooth bump

This file should construct or import the one-dimensional even smooth probability
bump used in the potential construction.

Target data:

* a nonnegative compactly supported smooth function `rho : R -> R`;
* support contained in `[-1, 1]`;
* evenness;
* integral equal to `1`;
* boundedness and positivity properties used for the constants in the paper.
-/

namespace L2Counterexample

end L2Counterexample
