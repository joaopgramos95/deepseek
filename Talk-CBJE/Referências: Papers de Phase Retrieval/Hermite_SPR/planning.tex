\documentclass[11pt]{amsart}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{hyperref}
\usepackage[shortlabels]{enumitem}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}

\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\newcommand{\C}{\mathbb{C}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\abs}[1]{\lvert #1 \rvert}
\newcommand{\norm}[1]{\lVert #1 \rVert}
\DeclareMathOperator{\dist}{dist}
\newcommand{\dd}{\mathrm{d}}
\newcommand{\ee}{\mathrm{e}}
\newcommand{\ii}{\mathrm{i}}
\newcommand{\rhoFn}{\rho}
\newcommand{\Fd}{\mathcal{F}_d}
\newcommand{\Sphere}{S^{2d-1}}
\newcommand{\Ann}[1]{\mathcal{A}_{#1}}

\title{Local Stable Phase Retrieval in Bargmann--Fock Space on $\C^d$}
\author{}

\begin{document}

\begin{abstract}
We were thinking something like explaining fock in dimension d very clearly, the general intuition of the proof, the actual proof, etc. We can state the full hermite and put in some comments on the proof. Thoughts?

My only rule is no LLMs to write the intro. We can get them to improve it later, but the intro should be written by humans, as it contains the message we want to convey
\end{abstract}

\maketitle
 
\section{Statement}
% Just a summary of conversations with Joaquim/ what was said in the threads at some point:
% \begin{itemize}
%     \item there should be a "default answer" on what math will probably look like from now on
%     \item Positive attitude: LLMs/Lean are exciting tools that allow us to concentrate on interesting problems coming from physics/other areas more
    % \item journal system? How should we organize the (possibly) massive amount of papers that is going to appear in a short amount of time
    % argue that the paper is "a dataset of proofs" not a document by humans for humans: these don't belong on Arxiv as dataset don't either. We do not pollute Arxiv with AI generated small improvements
    % \item "we put a paper with "Phase Retrieval in the Fock space"
    % which is 20-30 pages long, and explains SPR for hermite in C^d around any polynomial, discrete sampling if we have it and so on
    % terribly incomplete
    % and say the "Textual build artifact" is on github and contains all the proofs
    % with the guarantee that GPT5.4 can reproduce all the proofs correctly with the ideas in the 20-30 pages" (Jaume at some point)
    % https://arxiv.org/abs/2604.03789v1 was flagged as important to cite
    % \item also at some point do a "museum of failures" of what worked and what didn't?
% \end{itemize}


\end{document}
